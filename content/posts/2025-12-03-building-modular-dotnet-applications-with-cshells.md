---
title: "Building Modular .NET Applications with CShells"
slug: "building-modular-dotnet-applications-with-cshells"
description: "Building Modular .NET Applications with CShells A lightweight, modular shell system for multi-tenant .NET applications. For a while now I’ve wanted Elsa Workflows to support simple feature..."
publishedAt: "2025-12-03"
updatedAt: "2025-12-03"
status: "published"
authors:
  - "sipke"
category: "Engineering"
tags:
  - "modularity"
  - "aspnetcore"
  - "c-sharp-programming"
  - "multitenancy"
featuredImage: "https://cdn-images-1.medium.com/max/1024/1*hccxAtUbB0SNsjrh8yXc3Q.png"
featuredImageAlt: "Building Modular .NET Applications with CShells"
sourceName: "Medium"
sourceUrl: "https://sipkeschoorstra.medium.com/building-modular-net-applications-with-cshells-521782089a0b"
seoTitle: "Building Modular .NET Applications with CShells"
seoDescription: "Building Modular .NET Applications with CShells A lightweight, modular shell system for multi-tenant .NET applications. For a while now I’ve wanted Elsa Workflows to support simple feature..."
redirectFrom: []
---
<h3>Building Modular .NET Applications with CShells</h3><h4><em>A lightweight, modular shell system for multi-tenant .NET applications.</em></h4><figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*hccxAtUbB0SNsjrh8yXc3Q.png" /></figure><p>For a while now I’ve wanted <a href="https://docs.elsaworkflows.io/">Elsa Workflows</a> to support simple feature toggling and proper multi-tenancy where each tenant gets its own isolated set of services. Coming from the <a href="https://orchardcore.net/">Orchard Core</a> world, the shells concept has always made sense to me: wrap a service collection, let a shell manager rebuild it at runtime, and you get clean per-tenant isolation.</p><p><a href="https://www.cshells.io/">CShells</a> is a lightweight take on that idea, made to be easy to drop into any .NET application.</p><h3>What is CShells?</h3><p>CShells introduces the idea of shells, which are isolated DI containers with their own configuration and enabled features. In practice, CShells lets you:</p><ul><li>Define features as self-contained modules with their own services and endpoints.</li><li>Group features into shells representing tenants, environments, or pricing tiers.</li><li>Configure shells through JSON files, appsettings.json, or code.</li><li>Isolate dependencies per shell.</li><li>Add or update shells while the app is running.</li></ul><p>Each shell operates independently, so you can run multiple tenants or environments in a single application without conflicts.</p><h3>Why CShells?</h3><h4>How it started</h4><p>CShells didn’t come out of a production refactor. It started as something I knew I would eventually need for Elsa Workflows, even though I haven’t integrated it there yet. The goal was to have a clean, flexible way to enable or disable features without rebuilding Docker images or maintaining separate versions. CShells is still fresh, basically a preview release. But the more I worked on it, the more I figured it might be useful beyond the original Elsa-related idea. In a way, it mirrors Elsa’s own origin: just like Elsa grew out of a feature in Orchard Core, CShells now grows out of Orchard Core’s shells concept.</p><h4>1. Multi-tenant applications without custom plumbing</h4><p>Multi-tenant systems usually require a lot of custom work around routing, middleware, and service isolation. With CShells, each tenant is simply a shell with its own feature set and DI container. For example:</p><pre>// Tenant A gets Stripe payments and email notifications<br>{<br>  &quot;name&quot;: &quot;TenantA&quot;,<br>  &quot;features&quot;: [&quot;Core&quot;, &quot;StripePayment&quot;, &quot;EmailNotification&quot;],<br>  &quot;properties&quot;: {<br>    &quot;WebRouting&quot;: {<br>      &quot;Path&quot;: &quot;tenant-a&quot;<br>    }<br>  }<br>}</pre><pre>// Tenant B gets PayPal, SMS and fraud detection<br>{<br>  &quot;name&quot;: &quot;TenantB&quot;,<br>  &quot;features&quot;: [&quot;Core&quot;, &quot;PayPalPayment&quot;, &quot;SmsNotification&quot;, &quot;FraudDetection&quot;],<br>  &quot;properties&quot;: {<br>    &quot;WebRouting&quot;: {<br>      &quot;Path&quot;: &quot;tenant-b&quot;<br>    }<br>  }<br>}</pre><h4>2. Feature toggles with real modularity</h4><p>Feature flags work for simple switches, but they break down when features have their own services and endpoints. CShells treats features as proper modules with DI support and handles their internal dependencies automatically.</p><h4>3. Environment-specific configurations</h4><p>Even single-tenant apps can benefit from shells. You can define different shells for development, staging, and production with different features enabled. Debugging tools in dev but not in prod? Just configure a different shell per environment.</p><h4>4. Runtime reconfiguration</h4><p>CShells can add, update, or remove shells at runtime without restarting the app. This is great for SaaS platforms where new tenants or updated configurations need to be applied on the fly.</p><h3>How to use CShells</h3><h4>Step 1: Define your features</h4><p>Implement IWebShellFeature and decorate it with [ShellFeature].</p><pre>using CShells.Features;<br>using CShells.AspNetCore.Features;<br><br>[ShellFeature(&quot;Core&quot;, DisplayName = &quot;Core Services&quot;)]<br>public class CoreFeature : IWebShellFeature<br>{<br>    public void ConfigureServices(IServiceCollection services)<br>    {<br>        services.AddSingleton&lt;IAuditLogger, AuditLogger&gt;();<br>        services.AddSingleton&lt;ITimeService, TimeService&gt;();<br>    }<br>    public void MapEndpoints(<br>        IEndpointRouteBuilder endpoints,<br>        IHostEnvironment? environment)<br>    {<br>        endpoints.MapGet(&quot;&quot;, () =&gt; new { Message = &quot;Hello from Core&quot; });<br>    }<br>}</pre><p>Features can depend on other features:</p><pre>[ShellFeature(&quot;Weather&quot;, DependsOn = [&quot;Core&quot;])]<br>public class WeatherFeature : IWebShellFeature<br>{<br>    public void ConfigureServices(IServiceCollection services)<br>    {<br>        services.AddSingleton&lt;IWeatherService, WeatherService&gt;();<br>    }<br><br>    public void MapEndpoints(<br>        IEndpointRouteBuilder endpoints,<br>        IHostEnvironment? environment)<br>    {<br>        endpoints.MapGet(&quot;weather&quot;, <br>          (IWeatherService svc) =&gt; svc.GetForecast());<br>    }<br>}</pre><h4>Step 2: Configure your shells</h4><p>You can configure shells in JSON files:</p><pre>// Shells/Default.json<br>{<br>  &quot;name&quot;: &quot;Default&quot;,<br>  &quot;features&quot;: [&quot;Core&quot;, &quot;Weather&quot;]<br>}</pre><p>Or via appsettings:</p><pre>{<br>  &quot;CShells&quot;: {<br>    &quot;Shells&quot;: [<br>      {<br>        &quot;Name&quot;: &quot;Default&quot;,<br>        &quot;Features&quot;: [&quot;Core&quot;, &quot;Weather&quot;]<br>      }<br>    ]<br>  }<br>}</pre><p>Or in code:</p><pre>builder.AddShells(cshells =&gt;<br>{<br>    cshells.AddShell(&quot;Default&quot;, shell =&gt; shell<br>        .WithFeatures(&quot;Core&quot;, &quot;Weather&quot;);<br>});</pre><h4>Step 3: Wire it up in Program.cs</h4><pre>using CShells.AspNetCore.Extensions;<br><br>var builder = WebApplication.CreateBuilder(args);<br><br>// Register CShells. Reads from appsettings.json by default<br>builder.AddShells();<br><br>var app = builder.Build();<br>app.UseHttpsRedirection();<br>app.UseRouting();<br><br>// For routing HTTP requests to a shell.<br>app.MapShells();<br><br>app.Run();</pre><h3>Background services</h3><p>If you need background work per shell:</p><pre>public class ShellBackgroundWorker : BackgroundService<br>{<br>    private readonly IShellHost _shellHost;<br>    private readonly IShellContextScopeFactory _scopeFactory;<br><br>    public ShellBackgroundWorker(<br>        IShellHost shellHost,<br>        IShellContextScopeFactory scopeFactory)<br>    {<br>        _shellHost = shellHost;<br>        _scopeFactory = scopeFactory;<br>    }<br>    protected override async Task ExecuteAsync(CancellationToken stoppingToken)<br>    {<br>        while (!stoppingToken.IsCancellationRequested)<br>        {<br>            foreach (var shell in _shellHost.AllShells)<br>            {<br>                using var scope = _scopeFactory.CreateScope(shell);<br>                var service = scope.ServiceProvider<br>                    .GetService&lt;IMyService&gt;();<br>                service?.Execute();<br>            }<br>            await Task.Delay(TimeSpan.FromSeconds(30), stoppingToken);<br>        }<br>    }<br>}</pre><h3>Real-world use cases</h3><ul><li>Multi-tenant SaaS platforms</li><li>Modular monoliths</li><li>White-label deployments</li><li>API gateways / BFF layers</li><li>Environment-specific setups</li></ul><h3>Key benefits</h3><ul><li>Per-tenant features</li><li>Runtime reconfiguration</li><li>Dependency isolation</li><li>Clean architecture</li><li>Flexible configuration</li><li>Minimal boilerplate</li></ul><h3>Getting started</h3><p>CShells is open source on <a href="https://github.com/sfmskywalker/cshells">GitHub</a>. It includes a sample application (CShells.Workbench) that demonstrates multi-tenant feature configuration.</p><p>To try CShells:</p><ol><li>Install the NuGet package: CShells.AspNetCore</li><li>Read the docs at <a href="https://www.cshells.io/">https://www.cshells.io/</a></li><li>Explore the sample app</li></ol><p><a href="https://github.com/sfmskywalker/cshells/blob/main/LICENSE">CShells is MIT licensed</a> and actively maintained. Contributions and feedback are welcome on <a href="https://github.com/sfmskywalker/cshells">GitHub</a>.</p>